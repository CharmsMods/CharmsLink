## Composite Tab Fixes

Split below is based on the current live code state as of this pass, not just older notes.

## Fixed In Current Code

- after saving to library, autosave for open and changed composite projects fires wayy too often with "Autosaved "composite 1" to the Library." in the logs piling up, after every change, which makes the UI stutter for a few frames, which is annoying so the save time should be increased to 10 seconds, and not happen after every movment. also just run a quick check to make sure it's not saving a new copy every time it saves, (it doesnt look like it does but always good to make sure as this would definately blow up the library)

- we also need the ablity to add more than just squares to the canvas, include circles, triangles.

- holding down middle click and dragging on the canvas should pan the user around even tho they may not be *not hovering over something*, and the right click while not hovering over something should be remapped to clicking and dragging to select objects

- the user should be able to delete 1 or more selected objects by pressing delete

- when multiple objects are selected, a math mathmatical rectangle should be invisibly constructed perfectly around all selected objects, just encasing them, and then new handles along that rectangle should appear, allowing the user to transform all selected objects at once, with the same transform modes as normal, and the same transform options in the side bar, with the engine treating them like one big object, then once they are deselected, they go back to being treated like normal indevidual objects again.

- the composite tab needs its own settings catagory and settings to go along with it.
  current code note: this now lives in the site-wide `Settings` tab as the `Composite` category, not as a local Composite sidebar view.

- when saving a composite project to the library, it looks like it doenst include a thumnail for the project.
  current pass note: Composite library save now generates a preview fallback from the captured export blob when preview capture fails, and save updates preserve the existing saved preview/blob instead of wiping the thumbnail on a bad capture.

- there is not always a thumbnail for composite projects in the library, make sure this is saved for the project. and when it is saved, make sure it's updated when the project is updated.
  current pass note: handled by the same save-path hardening above, so Composite thumbnails should now be created more reliably and should not disappear on update when a preview capture flakes.

- the stretch scale mode for allowing a user to stretch or squish a canvas object in a direction, doesnt show up anywhere that i can see.
  current pass note: the `Transform` panel now surfaces resize modes as visible buttons instead of relying on the hidden-feeling selector alone, and stretch-capable layers explicitly explain that they can squish/stretch from the opposite side.

- cant seem to scale images down using the handles past a certian limit, this also goes along with the canvas zoom limit, i want the engine to stop thinking in absolute scaling for the zoom and images, and instead just let the user zoom in and out as much as they want, it doesnt matter if they zoom far in and build their project there, because they can set the resolution of the export, since the canvas that we are laying stuff out on shouldnt have a resolution, its just a 2D surface for us to work on.
  current pass note: Composite zoom/scale clamps were widened significantly in the document model so handle scaling and viewport zoom are much less artificially boxed in.

- fix the saving logic, linking logic, and loading logic between the composite tab and the editor tab, so it's more seemless. here is a scenario: lets say the user already has a project open on the editor tab, and then they create a composite project with some layers that come from other editor projects, then they have something on the canvas selected and decide they want to edit it a little bit, so they just click on the editor tab. the site should have a popup that asks if they want to save the current project that's already open in the editor, which the site does do this. however, lets say they say either no or yes it doenst reall matter because we dont care about that project right now; now the user is on the editor tab with the selected thing they had on the composite canvas, open in the editor, lets say they make a change to the editor pipline or settings or anything on the editor site with relation to the loaded project, and now they click back on the composite tab, when they load back into the composite tab, those edits that they made should be updated and shown on that layer, since the user was just editing it. now lets say they arent happy with the edits they made and want to go back and change it again/some more, when the click on the editor tab they should not get a "do you want to save the current project" because it's the same thing they were just editing a moment ago, and technically it's been saved anyways because the user just saw those changes on the object on the composite canvas. now lets say the select something else, repeat the whole thing, and come back to the original item they selected for editor editing, the site should just load it like it would any of the other composite objects and allow editing. this is how i want the link between them to work.
  current pass note: opening a linked Editor layer from Composite now skips the extra save prompt when the current Editor document already matches one of the Composite-linked Editor layers, so switching between linked layers is more seamless.

- when trying to do an export with custom bounds and some large pictures from editor projects, i get "Failed to fetch" in the logs tab, and the png export fails, and i am not sure what this is due to.
  current pass note: Composite export/save paths and the shared worker data-url helper no longer rely on `fetch(dataUrl)` for huge embedded image payloads, and now decode `data:` URLs directly. this should address the large custom-bounds `Failed to fetch` case.

- when clicking the load button on a composite project in the library, sometimes nothing happens and the project just doenst load, and or the 2 bars at the top of the screen that make up the navigation and settings for sites and stuff, will turn super washed out or super dark in color like the site is loading something, but nothing ever happens, usually if something does happen, after all of that the site will hit the library loading screen with the text (text from my testing) "Loading Library project...
Validating "composite 1" and opening it in Composite", after that it just doenst do anything, and the slider doesnt progress, and no warnings or errors hit the logs tab.
  current pass note: the Library project load path no longer does a full `JSON.stringify` + `JSON.parse` clone of the entire payload up front, and top-level Library-envelope stripping no longer deep-clones the full project payload just to remove metadata fields. this should reduce the main-thread stall/hang behavior on large Composite Library loads.

- any regular uploaded images; the user should be able to convert those into an editor project and edit them for the canvas just like other editor projects, as updates to that image converted into an editor project are made, whenever the composite project saves to the library, it will of course save all assets and projects needed to reconstruct it, but also that new editor project that was made from an uploaded image on to the compositor canvas, then edited in the editor tab; it should be saved as a new seperate editor project in normal format like the rest and updated at the same time as the composite project saves to the library.
  current pass note: plain Composite image layers can now be converted into linked Editor-project layers and opened directly in Editor. generated converted layers are marked in Composite state, inherit manual Editor saves back into Composite, and are persisted as separate normal Editor Library projects when the Composite project saves to the Library.

- when importing multiple editor projects as canvas objects, the handles they get assigned dont always conform to the actual image shape, and then ruin the render because the image then conforms to the handles instead of the other way around, meaning the user could set the image where they want it, but the handles for it might be super small and not around the edges of the image, and then on png render, the image is stretched to fit the handles that are in the wrong spot.
  current pass note: editor-backed Composite layers now persist an explicit authoritative render width/height in layer source metadata, and the live stage plus both export paths use that stored render size instead of trusting the decoded preview bitmap dimensions. this is meant to stop imported Editor layers from inheriting thumbnail-like or stale preview sizing and then stretching on export.

- currently, popups for saving things like the site asking you if you want to save before doing something new and that kind of thing; the user cant actually click on the button(s) because they just interact with the site behind the popup, which is usually the open composite canvas, so if i move the area from behind the popup then i can get to the button, but this is just getting around the problem, the popups shouldn't have to go around the rest of the UI, they're popups! and should work normally.
  current pass note: the shared app-shell now enters an explicit `is-app-dialog-open` modal state while the save/confirm dialog is open, which disables pointer interaction to the background UI and keeps dialog focus inside the popup instead of relying only on z-index layering.

  - sliders do update in real time with things which is great when im draggin things, however i cant click and drag sliders, they only respond to clicks along their line where the slider dot is. the same sort of bug applies to the text boxes for entering values, where i click in the box, the either type one thing, or delete something, and the box is immediately unfocused. this goes for the content of the text box objects as well.
  current pass note: Composite panel interaction locking was tightened again so the sidebar should stop rebuilding while a control is actively being dragged or edited. this needs retesting in the live app.

- the logic for the export should be updated to understand, it's likely that whatever the user wants to render/export, is going to be on their screen visible on the canvas when they click on the render tab, so make sure that the area for the render area isnt super big because it thinks it needs to cover this perhaps huge background object that's just being used for color, and is way bigger than the currently observable canvas.
  current pass note: custom export seeding now prefers the union of actually visible clipped layer coverage inside the current viewport, instead of only intersecting whole-document bounds with the viewport. very large background-like layers that cover almost the whole viewport are ignored for seeding if smaller visible layers are also on screen. this still needs live retesting.

- exports should be as large as the user wants in terms of resolution, and i want you to do some research into GPU vs CPU rendering for very specifically this part only, since the GPU will have a texture size limit and crash the site if it's larger than that, but if its CPU rendered, we can have something much larger, but it's just slower, and it would also be a good idea to look into whether or not web workers could be and should be impliemnted to render huge resolution images off the canvas for the composite tab even if its quite slow.
perhaps having both, where images under a certain resolution are GPU rendered and settings above a resoltion, are CPU rendered, with or without web workers depending on research and how things actually work. there could also be a manual toggle for the user to choose between these 2 options overriding the automatic detection.
  current pass note: current Composite export is still CPU 2D canvas in both paths, but the app now does the research-backed part of this safely: `Settings > Composite` shows worker readiness plus probed WebGL limits (`MAX_TEXTURE_SIZE`, viewport limits, GPU-safe edge), manual backend override still exists, and `Auto` is now size-aware instead of blindly preferring the worker. smaller exports stay on the main thread, larger exports route to the background worker, and the export log explains which path was chosen. the future "real GPU/WebGL renderer for smaller exports" part is still not implemented because that renderer split does not exist in the current Composite architecture yet.

## Still Open / Still Needs Work / Ideas

- Allow the user to crop images using handles
  current pass note: image and editor-project Composite layers now support a dedicated crop mode that reuses the on-canvas selection handles for non-destructive cropping, including rotated/flipped layers, plus reset/exit controls in the Transform panel.


fonts on text settings is a text box instead of what it should be as a drop down box with options
current pass note: the Composite text `Font Family` control now uses the shared editor font dropdown options instead of a freeform text field.



the user should be able to click on a object/layer on the canvas, and the click "Replace With" and that will open up a popup menu that ties into the existing buttons that either add a normal image to the canvas, or a editor project to the canvas, and instead of just adding one of either, whatever is selected will replace the selected object/layer on the canvas, with the same properties, position, rotation, scale, opacity, blend mode, etc. but with the new image or editor project. this means FIRST, the issue where clicking on a part of the canvas that doenst have anything on it, doesnt deselect everything. just for clarification, if something on the canvas is selected, and the user clicks on either add editor project or add normal image, this wont go into replacement mode, replacement mode only happens when the user clicks on a new button called "Replace With" that should only appear when something is selected.
current pass note: Composite now shows a conditional `Replace With` toolbar action when a layer is selected. that opens a dedicated replacement chooser which routes into the existing Editor-project picker or image picker in explicit replacement mode only, while normal `Add Editor Project` / `Add Images` still keep their old additive behavior. replacement keeps the selected layer slot and preserves generic transform/appearance fields like position, rotation, scale, opacity, and blend mode. empty left-click on unused stage space now clears the selection cleanly instead of falling into an implicit pan start.
